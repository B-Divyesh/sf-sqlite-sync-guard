# Bundled demo sample

`sample.sql` records the schema and row created by `sqlite-sync-guard demo`. The command creates both closed and unsafe SQLite sets inside a new temporary directory, then runs the real scanner and exporter.
